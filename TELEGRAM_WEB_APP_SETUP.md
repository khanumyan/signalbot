# Настройка Telegram Web App

Это руководство поможет вам настроить интеграцию вашего приложения с Telegram, чтобы пользователи могли открывать его прямо из Telegram бота.

## Шаги настройки

### 1. Настройка переменных окружения

Убедитесь, что в файле `.env` указан правильный URL вашего приложения:

```env
APP_URL=https://your-domain.com
```

**Важно:** URL должен быть доступен по HTTPS (обязательно для Telegram Web App).

### 2. Настройка меню бота

Выполните команду для настройки меню бота:

```bash
php artisan telegram:setup-menu
```

Эта команда:
- Установит кнопку меню в боте
- Настроит команду `/start`
- Добавит кнопку "📊 Открыть приложение" в меню бота

### 3. Настройка Webhook (опционально)

Если вы хотите получать обновления от Telegram через webhook, настройте его:

1. Убедитесь, что ваш сервер доступен из интернета
2. Установите webhook URL в настройках бота через BotFather или API:

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://your-domain.com/telegram/webhook"}'
```

### 4. Проверка работы

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте команду `/start`
4. Нажмите на кнопку меню (три линии) в боте
5. Должна появиться кнопка "📊 Открыть приложение"
6. Нажмите на неё - приложение откроется в Telegram

## Функции Telegram Web App

Приложение автоматически определяет, запущено ли оно в Telegram, и предоставляет следующие возможности:

- **Автоматическое определение пользователя** - доступ к данным пользователя через `window.getTelegramUser()`
- **Настройка темы** - автоматическая настройка цветов под темную тему
- **Кнопки управления** - возможность показать/скрыть главную кнопку и кнопку "Назад"
- **Уведомления** - использование нативных уведомлений Telegram

## API функции

В JavaScript доступны следующие функции:

```javascript
// Получить данные пользователя
const user = window.getTelegramUser();

// Показать главную кнопку
window.showTelegramMainButton('Текст кнопки', () => {
    // Действие при клике
});

// Скрыть главную кнопку
window.hideTelegramMainButton();

// Показать кнопку "Назад"
window.showTelegramBackButton(() => {
    // Действие при клике
});

// Скрыть кнопку "Назад"
window.hideTelegramBackButton();

// Показать alert
window.showTelegramAlert('Сообщение', () => {
    // Callback после закрытия
});

// Показать confirm
window.showTelegramConfirm('Вопрос?', (confirmed) => {
    if (confirmed) {
        // Пользователь подтвердил
    }
});

// Закрыть приложение
window.closeTelegramApp();

// Открыть ссылку
window.openTelegramLink('https://example.com');
```

## Отладка

Если приложение не открывается в Telegram:

1. Проверьте, что URL доступен по HTTPS
2. Убедитесь, что домен не заблокирован
3. Проверьте консоль браузера на наличие ошибок
4. Убедитесь, что меню бота настроено правильно

## Дополнительная информация

- [Документация Telegram Web Apps](https://core.telegram.org/bots/webapps)
- [Telegram Bot API](https://core.telegram.org/bots/api)




