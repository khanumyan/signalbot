# Быстрое тестирование Telegram Web App

## ⚡ Быстрый старт (5 минут)

### Шаг 1: Установите ngrok (если еще не установлен)

```bash
# Ubuntu/Debian
sudo snap install ngrok

# Или скачайте с https://ngrok.com/download
# Или через npm: npm install -g ngrok
```

### Шаг 2: Запустите Laravel приложение

```bash
php artisan serve
```

Приложение будет доступно на `http://localhost:8000`

### Шаг 3: Запустите ngrok (в новом терминале)

```bash
ngrok http 8000
```

Вы увидите что-то вроде:
```
Forwarding  https://abc123.ngrok.io -> http://localhost:8000
```

**Скопируйте HTTPS URL** (например: `https://abc123.ngrok.io`)

### Шаг 4: Обновите .env файл

Откройте `.env` и измените:

```env
APP_URL=https://abc123.ngrok.io
```

**Важно:** Используйте HTTPS URL из ngrok!

### Шаг 5: Настройте меню бота

```bash
php artisan telegram:setup-menu
```

Должно вывести: `✅ Меню бота успешно настроено!`

### Шаг 6: Запустите тест

```bash
php artisan telegram:test-webapp
```

Все проверки должны пройти успешно ✅

### Шаг 7: Откройте в Telegram

1. Откройте Telegram
2. Найдите вашего бота (по имени или через поиск)
3. Отправьте команду `/start`
4. Нажмите на **кнопку меню** (три горизонтальные линии) в правом верхнем углу
5. Должна появиться кнопка **"📊 Открыть приложение"**
6. Нажмите на неё
7. Приложение откроется прямо в Telegram! 🎉

## 🔍 Проверка работы

### В консоли браузера (F12 в Telegram)

Откройте DevTools (F12) и выполните:

```javascript
// Проверка Telegram Web App
console.log(window.tg); // Должен вывести объект

// Получить данные пользователя
const user = window.getTelegramUser();
console.log(user); // Должен вывести ваши данные из Telegram
```

### Тестирование функций

```javascript
// Показать alert
window.showTelegramAlert('Тест работает!', () => {
    console.log('Alert закрыт');
});

// Показать confirm
window.showTelegramConfirm('Вы уверены?', (confirmed) => {
    console.log('Подтверждено:', confirmed);
});
```

## ❌ Решение проблем

### Ошибка: "Only HTTPS links are allowed"

**Причина:** APP_URL использует HTTP вместо HTTPS

**Решение:**
1. Используйте ngrok для получения HTTPS URL
2. Обновите APP_URL в .env на HTTPS URL

### Ошибка: "Меню бота не настроено"

**Решение:**
```bash
php artisan telegram:setup-menu
```

### Приложение не открывается

**Проверьте:**
1. ✅ ngrok запущен и показывает HTTPS URL
2. ✅ APP_URL в .env указывает на HTTPS URL из ngrok
3. ✅ Laravel приложение запущено (`php artisan serve`)
4. ✅ Меню настроено (`php artisan telegram:setup-menu`)

### Кнопка меню не появляется

**Решение:**
1. Перезапустите Telegram (закройте и откройте снова)
2. Проверьте, что меню настроено: `php artisan telegram:setup-menu`
3. Убедитесь, что вы используете правильного бота

## 📝 Полезные команды

```bash
# Тест подключения
php artisan telegram:test

# Тест Web App интеграции
php artisan telegram:test-webapp

# Настройка меню
php artisan telegram:setup-menu

# Информация о боте
php artisan telegram:info
```

## 🎯 Чек-лист

- [ ] ngrok установлен и запущен
- [ ] Laravel приложение запущено (`php artisan serve`)
- [ ] APP_URL в .env указывает на HTTPS URL из ngrok
- [ ] Меню настроено (`php artisan telegram:setup-menu`)
- [ ] Тест проходит успешно (`php artisan telegram:test-webapp`)
- [ ] Бот найден в Telegram
- [ ] Команда `/start` отправлена
- [ ] Кнопка меню открыта
- [ ] Кнопка "📊 Открыть приложение" видна
- [ ] Приложение открывается в Telegram

## 💡 Совет

Для постоянного использования на продакшн сервере:
1. Используйте реальный домен с HTTPS
2. Обновите APP_URL в .env
3. Выполните `php artisan telegram:setup-menu`




