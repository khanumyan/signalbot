# Руководство по тестированию Telegram Web App

## Быстрый старт

### 1. Запустите тестовую команду

```bash
php artisan telegram:test-webapp
```

Эта команда проверит:
- ✅ Подключение к Telegram боту
- ✅ Настройку APP_URL
- ✅ Доступность приложения
- ✅ Настройку меню бота
- ✅ Наличие необходимых файлов

### 2. Настройте меню бота

```bash
php artisan telegram:setup-menu
```

### 3. Тестирование в Telegram

#### Вариант A: Локальное тестирование с ngrok

1. **Установите ngrok** (если еще не установлен):
   ```bash
   # Ubuntu/Debian
   sudo snap install ngrok
   
   # Или скачайте с https://ngrok.com/download
   ```

2. **Запустите Laravel приложение**:
   ```bash
   php artisan serve
   ```

3. **Запустите ngrok** в отдельном терминале:
   ```bash
   ngrok http 8000
   ```

4. **Скопируйте HTTPS URL** из ngrok (например: `https://abc123.ngrok.io`)

5. **Обновите .env файл**:
   ```env
   APP_URL=https://abc123.ngrok.io
   ```

6. **Настройте меню бота**:
   ```bash
   php artisan telegram:setup-menu
   ```

7. **Откройте Telegram**:
   - Найдите вашего бота
   - Отправьте `/start`
   - Нажмите на кнопку меню (три линии)
   - Должна появиться кнопка "📊 Открыть приложение"
   - Нажмите на неё

#### Вариант B: Тестирование на продакшн сервере

1. Убедитесь, что `APP_URL` в `.env` указывает на ваш HTTPS домен
2. Выполните `php artisan telegram:setup-menu`
3. Откройте бота в Telegram и используйте кнопку меню

## Проверка работы функций

### В консоли браузера (F12)

Откройте консоль разработчика в Telegram и проверьте:

```javascript
// Проверка, что Telegram Web App загружен
console.log(window.tg); // Должен вывести объект Telegram Web App

// Получить данные пользователя
const user = window.getTelegramUser();
console.log(user); // Должен вывести данные пользователя

// Проверить, что мы в Telegram
console.log(window.tg.initDataUnsafe); // Должен вывести данные инициализации
```

### Тестирование функций

```javascript
// Показать главную кнопку
window.showTelegramMainButton('Тест', () => {
    alert('Кнопка нажата!');
});

// Показать alert
window.showTelegramAlert('Тестовое сообщение', () => {
    console.log('Alert закрыт');
});

// Показать confirm
window.showTelegramConfirm('Вы уверены?', (confirmed) => {
    console.log('Подтверждено:', confirmed);
});
```

## Проверка страниц

Убедитесь, что все страницы загружают скрипт Telegram Web App:

1. ✅ Главная страница (`/`)
2. ✅ История сигналов (`/signals`)
3. ✅ Детали сигнала (`/signals/{id}`)
4. ✅ Настройки стратегий (`/strategy-settings`)
5. ✅ Анализ графиков (`/chart-analysis`)

## Отладка

### Проблема: Приложение не открывается в Telegram

**Решение:**
1. Проверьте, что URL использует HTTPS
2. Убедитесь, что домен не заблокирован
3. Проверьте логи: `storage/logs/laravel.log`
4. Выполните: `php artisan telegram:test-webapp`

### Проблема: Кнопка меню не появляется

**Решение:**
1. Выполните: `php artisan telegram:setup-menu`
2. Проверьте токен бота в `.env` или `TelegramService.php`
3. Убедитесь, что бот активен

### Проблема: JavaScript функции не работают

**Решение:**
1. Проверьте консоль браузера на ошибки
2. Убедитесь, что файл `public/js/telegram-web-app.js` существует
3. Проверьте, что скрипт загружается на странице (Network tab в DevTools)

### Проблема: Данные пользователя недоступны

**Решение:**
1. Убедитесь, что приложение открыто через Telegram (не в обычном браузере)
2. Проверьте, что скрипт `telegram-web-app.js` загружается ДО других скриптов
3. Проверьте консоль на ошибки

## Тестирование webhook (опционально)

Если вы настроили webhook:

```bash
# Проверка webhook
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getWebhookInfo"

# Установка webhook
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://your-domain.com/telegram/webhook"}'
```

## Полезные команды

```bash
# Тест подключения к боту
php artisan telegram:test

# Тест Web App интеграции
php artisan telegram:test-webapp

# Настройка меню
php artisan telegram:setup-menu

# Информация о боте
php artisan telegram:info
```

## Чек-лист перед продакшн

- [ ] APP_URL использует HTTPS
- [ ] Меню бота настроено (`php artisan telegram:setup-menu`)
- [ ] Все страницы загружают `telegram-web-app.js`
- [ ] Тестовая команда проходит успешно
- [ ] Приложение открывается в Telegram
- [ ] Данные пользователя доступны
- [ ] Все функции работают корректно




